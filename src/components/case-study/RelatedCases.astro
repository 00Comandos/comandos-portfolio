---
import { Image } from "astro:assets";
import type { Project } from "~/data/projects";

type Props = { cases: readonly Project[] };
const { cases } = Astro.props;
---

<section
  aria-labelledby="related-heading"
  class="pt-16 md:pt-20 pb-24 border-t border-[color:var(--color-border)]"
>
  <div class="cs-body">
    <p class="text-xs font-semibold tracking-[0.14em] uppercase text-[color:var(--color-muted)]">
      Keep exploring
    </p>
    <h2
      id="related-heading"
      class="mt-2 text-[1.5rem] md:text-[1.875rem] font-extrabold leading-tight tracking-tight"
    >
      Cases that might interest you
    </h2>

    <ul class="mt-10 grid gap-8 md:grid-cols-3">
      {
        cases.map((c) => (
          <li>
            <a href={`/projects/${c.slug}`} class="related-card group block">
              <div class="aspect-[4/5] overflow-hidden rounded-lg bg-[color:var(--color-paper-soft)] relative">
                <Image
                  src={c.image}
                  alt={`${c.brand} — ${c.title}`}
                  widths={[240, 360, 480]}
                  sizes="(min-width: 768px) 240px, 100vw"
                  loading="lazy"
                  decoding="async"
                  class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-[1.04]"
                />
                <span class="absolute top-3 left-3 bg-[color:var(--color-paper)] text-[0.75rem] font-semibold px-3 py-1 rounded-full shadow-[0_1px_2px_rgba(0,0,0,0.12)]">
                  {c.brand}
                </span>
              </div>
              <h3 class="mt-4 text-[1rem] font-bold leading-snug group-hover:opacity-70 transition-opacity">
                {c.title}
              </h3>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</section>
